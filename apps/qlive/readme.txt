1,主播:
    0=禁用,1=启用,2=待分配(后台添加)/申请中(前台申请)
    前台申请,默认状态是2
    后台增加主播,默认状态是2;添加之后如果修改主播,只能选择0和1,不可将已分配房间的主播选择2
    后台开通新房间,选择申请中的主播即为通过申请;
    可在主播列表选择启用,禁播该主播;

2,房间:
    后台新增房间;可以选择主播进行绑定
    后台开通新房间,选择申请中的主播即为通过申请;
    新增房间时,生成一个推流码,主播可以在个人中心获取
    在列表页可以禁用,启用该房间;

3,直播间的个性化设置均在主播端进行设置;

